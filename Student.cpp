#include "Student.h"

Student::Student(std::string name, std::string middle_name, std::string last_name,
                 std::string group_name, size_t grade) : name(name), middle_name(middle_name),
                                                         last_name(last_name), group_name(group_name), grade(grade) {

}

